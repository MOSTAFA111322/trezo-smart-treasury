# دليل الأمان التشغيلي والاستعادة — TREZO

## نطاق الدليل

هذا الدليل يصف الإجراءات الآمنة لمراجعة نشاط المستخدمين، والتحقق من النسخ الاحتياطية، واختبار الاستعادة خارج بيئة الإنتاج. لا يحتوي على مفاتيح أو كلمات مرور أو رموز جلسات، ولا ينفذ أي تعديل على قاعدة البيانات أو التخزين.

## مراجعة النشاط داخل TREZO

يعتمد مركز المتابعة داخل التطبيق على طلبات الصرف المفتوحة وسجل التدقيق الحالي. يراجع المدير يومياً الطلبات في حالات المسودة والمراجعة والاعتماد، ثم يفتح سجل التدقيق للتحقق من عمليات تغيير الأدوار والصلاحيات والكيانات وانتقالات دورة الاعتماد.

ينبغي اعتبار سجل التدقيق هو المرجع التشغيلي للتغييرات الحساسة. لا يتم اعتبار الإشعار بديلاً عن الاعتماد الرسمي؛ فهو وسيلة متابعة فقط، بينما تبقى انتقالات الطلبات محكومة بالعقود الخادمية الحالية.

## بيانات الجلسات ومحاولات الدخول

تُراجع قائمة المستخدمين من شاشة المستخدمين والصلاحيات، مع التركيز على اسم المستخدم والبريد والدور وآخر وقت تسجيل دخول. لا تُحفظ كلمات المرور أو رموز الوصول داخل التطبيق ولا ينبغي تصديرها أو إرسالها في التقارير. عند الاشتباه بنشاط غير معتاد، يتم تعطيل الوصول من مزود الهوية أو لوحة الإدارة بعد توثيق السبب في سجل التدقيق الإداري.

## النسخ الاحتياطي المطلوب من مالك المشروع

يجب أن تشمل النسخة المستقلة قاعدة البيانات، كائنات التخزين، وبيانات المستخدمين الوصفية اللازمة للربط المستقبلي فقط. تحفظ النسخ في مخزن منفصل ومشفر، مع تحديد تاريخ الإنشاء، مصدر النسخة، نطاقها، وسياسة الاحتفاظ بها.

## التحقق من النسخة

بعد إنشاء النسخة، يتحقق المالك من اكتمال ملف قاعدة البيانات، وعدد جداول المخطط، وقابلية قراءة سجل التخزين الوصفي، وعدم وجود أسرار مكشوفة في الملفات أو أسماء الملفات. تحفظ قيمة checksum لكل ملف، ثم يعاد حسابها عند النقل أو الاستعادة.

## اختبار الاستعادة

ينفذ الاختبار في مشروع Supabase أو قاعدة منفصلة غير إنتاجية. يستعاد مخطط قاعدة البيانات أولاً، ثم البيانات، ثم كائنات التخزين، وبعدها تجرى اختبارات قراءة فقط: تسجيل الدخول، قراءة الشركات والعملات والسنوات، فتح طلب صرف، قراءة سجل التدقيق، وتنزيل مرفق اختباري. يمنع توجيه الاختبار إلى عنوان الإنتاج، ويمنع تنفيذ تحديثات أو حذف أثناء التحقق.

## ما يجب عدم كشفه

لا تُرسل أو تُطبع مفاتيح Service Role، كلمات مرور PostgreSQL، JWT، رموز OAuth، ملفات البيئة، روابط الاتصال الكاملة، أو رموز الجلسات. عند مشاركة تقرير، تستخدم أسماء المتغيرات أو عبارة `FOUND — value redacted` فقط.

## معيار الجاهزية

تعتبر الاستعادة قابلة للتحقق فقط عندما تنجح مطابقة checksum، وتنجح اختبارات القراءة الأساسية، وتظهر البيانات المرجعية والطلبات وسجل التدقيق والمرفقات في البيئة المنفصلة، ويوقع المالك على نتيجة الاختبار وتاريخه.

> هذا الملف إرشادي فقط. لا ينفذ Backup أو Restore تلقائياً ولا يغير إنتاج TREZO.

## مرجع Manus الرسمي لنسخ مواقع WebDev

إذا كان المشروع مشمولاً بإشعار خدمة Manus، فنسخة الكود وحدها لا تكفي؛ يجب إنشاء **Task Data Backup** لأنه يشمل كود الموقع والملفات وقاعدة البيانات والإعدادات والأسرار والتكاملات. كل تصدير هو لقطة زمنية ثابتة ولا يتزامن تلقائياً مع التغييرات اللاحقة.

لأوسع نسخة: افتح [أداة النسخ الرسمية](https://manus.im/backup)، ثم اختر **Export task data → Export more → All tasks → All time → Start export**. ولنسخة المواقع فقط اختر **Custom export → Website tasks → All time → Start export**. يجب الاحتفاظ بكل الحزم الكاملة دون إعادة تسمية أو خلط أجزاء من صادرات مختلفة.

وفق الإرشادات الرسمية، موعد النسخ المذكور هو **23 أغسطس 2026، الساعة 7:59 صباحاً بتوقيت سنغافورة**، وتفتح الاستعادة في **25 أغسطس 2026، الساعة 8:00 صباحاً بتوقيت سنغافورة**. الاستعادة تتم مرة واحدة؛ لذلك يجب التأكد من امتلاك الحزم الصحيحة والكاملة قبل التنفيذ. راجع [دليل النسخ](https://help.manus.im/en/articles/16147892-service-change-overview-how-to-back-up-your-data) و[دليل الاستعادة](https://help.manus.im/en/articles/16147895-service-change-overview-how-to-restore-your-data) للتحقق من حالة الحساب ونوعه قبل الإجراء.

هذا المرجع لا ينفذ نسخة أو استعادة تلقائياً، ولا يغير قاعدة بيانات TREZO أو إنتاجه.
