# تقرير الجاهزية النهائي لنظام TREZO Smart Treasury

**تاريخ المراجعة:** 21 أغسطس 2026

**نطاق المراجعة:** مراجعة شاملة للكود، مسارات الخادم، الصلاحيات، دورة الاعتماد، المرفقات، التقارير والطباعة، المعاينة المتجاوبة، سجلات التشغيل، TypeScript، الاختبارات، وProduction Build.

> **الخلاصة التنفيذية:** النظام في حالة تقنية مستقرة وقابل للتشغيل في بيئة الاختبار، مع نجاح الفحوصات البرمجية الأساسية وإصلاح عيب تزامن مهم في دورة الاعتماد. لا أوصي بتحويله إلى تشغيل مالي فعلي قبل تنفيذ بوابة التسليم البشرية: نسخة احتياطية مستقلة قابلة للتحقق، تنظيف بيانات الاختبار بعد موافقة صريحة، وإنشاء جلسات مصادقة منفصلة للأدوار الثلاثة المطلوبة واختبار الدورة الفعلية والطباعة.

## 1. نطاق الأدلة التي تمت مراجعتها

تمت مراجعة الملفات الأساسية في `client/src` و`server` و`shared` و`drizzle`، مع التركيز على `server/routers.ts` و`client/src/pages/Workspace.tsx` و`client/src/components/InternalEmployeesPanel.tsx` و`client/src/_core/hooks/useAuth.ts` وملفات الاختبارات وسجل المهام. كما تمت مراجعة سجلات الخادم والمتصفح وطلبات الشبكة، والتقاط معاينة كاملة على سطح المكتب بعرض 1280 بكسل، ومعاينة كاملة على الهاتف بعرض 390 بكسل.

## A) مكتمل ويعمل

| المجال | النتيجة | الدليل التشغيلي |
|---|---|---|
| الصحة البرمجية | TypeScript يمر دون أخطاء | `pnpm exec tsc --noEmit` أعاد رمز نجاح 0 |
| الاختبارات | 29 ملف اختبار، 70 اختباراً ناجحاً | `pnpm exec vitest run`: 29 passed / 70 passed |
| البناء | Production Build ناجح | `pnpm run build` اكتمل بنجاح، وتم إنشاء `dist/index.js` وملفات الواجهة |
| دورة الاعتماد | المسار المؤسسي موجود ومختبر | `draft → review → approved → executed` مع حالات الرفض وإعادة الطلب للمسودة |
| الترقيم المرجعي | الزيادة الذرية موجودة | حارس مقارنة `nextValue` يمنع حجز رقمين متطابقين عند الإنشاء المتزامن |
| قناة الصرف | قواعد البنك والصراف محمية خادمياً | قناة البنك تتطلب حساباً بنكياً فعالاً مرتبطاً بالمستفيد، والصراف لا يعيد حساباً بنكياً |
| المرفقات | الوصول يتم عبر مسار خادم محمي | توجد اختبارات لرفض المستخدم غير المخول ونجاح المالك أو المدير |
| الأدوار | أدوار تشغيلية منفصلة عن هوية الدخول | سجل الموظفين الداخلي يتيح التعيين دون إنشاء بريد أو كلمة مرور تلقائياً |
| تسجيل الخروج | تمت معالجة استمرار الجلسة في المعاينة | مسح حالة الجلسة المحلية، حارس خروج، إبطال الاستعلام، وإعادة تحميل صريحة |
| الواجهة | RTL ووضع ليلي واستجابة للجوال | المعاينة المكتبية والهاتفية أظهرت تخطيطاً قابلاً للاستخدام دون شاشة بيضاء |
| التقارير والطباعة | مركز التقارير والقوالب الرسمية موجودان | التصفية والتصدير ومسارات المعاينة والطباعة ممثلة في الواجهة والاختبارات |

## B) يعمل لكنه يحتاج تحسيناً قبل التشغيل الفعلي

أولاً، تظهر في المعاينة الحالية بيانات اختبار تشغيلية، منها طلب صرف تجريبي برقم مرجعي ظاهر في لوحة المؤشرات. هذا ليس عيباً برمجياً، لكنه يمنع اعتبار البيئة جاهزة مباشرة للتسليم المالي. يجب أخذ نسخة احتياطية مستقلة، ثم تحديد البيانات التجريبية بدقة، ثم تنظيفها أو أرشفتها بموافقة مالك النظام وفق إجراء منفصل.

ثانياً، الاختبار البرمجي يغطي الصلاحيات والمنطق، لكنه لا يثبت دورة عمل كاملة عبر ثلاث جلسات مصادقة بشرية مستقلة. يلزم اختبار عملي بحساب محاسب، وحساب مدير مالي، وحساب مدير عام، ثم التحقق من المراجع اللاحق والطباعة وقناتي البنك والصراف.

ثالثاً، Production Build ناجح، لكنه يصدر تحذيراً من حجم حزمة JavaScript الرئيسية الأكبر من 500 كيلوبايت. لا يمنع ذلك البناء أو التشغيل، لكنه يمثل فرصة تحسين أداء لاحقة عبر تقسيم الحزم أو التحميل الكسول، ولا ينبغي إصلاحه بإعادة تصميم واسعة قبل التسليم.

رابعاً، يظهر تحذير بيئي من أن إعداد `pnpm` داخل `package.json` لم يعد يُقرأ في إصدار pnpm الحالي. لم يفشل البناء بسبب ذلك، لكن يُستحسن في دورة صيانة لاحقة نقل إعدادات `patchedDependencies` و`overrides` إلى ملف إعداد pnpm حديث بعد اختبار مستقل.

## C) غير مكتمل أو معلق خارجياً

| البند | الحالة الحالية | سبب التعليق |
|---|---|---|
| اختبار ثلاث جلسات حقيقية | معلق | البيئة الحالية لا توفر ثلاث هويات مصادق عليها مستقلة في الوقت نفسه |
| اختبار الصراف والبنك من البداية إلى الطباعة | معلق | يحتاج جلسات الأدوار الفعلية وبيانات تشغيل معتمدة |
| نسخة احتياطية قابلة للتحقق | معلق | لا توجد في البيئة الحالية بيانات اتصال إدارية مستقلة أو نسخة يمكن اختبار استعادتها |
| تنظيف بيئة الاختبار | لم يُنفذ | يتطلب نسخة احتياطية وموافقة صريحة؛ لم تُحذف أي بيانات في هذه المراجعة |
| قرار عزل الشركات | يحتاج قراراً تشغيلياً | بعض الملخصات والقوائم تستعلم على مستوى النظام، ويجب اعتماد سياسة رؤية البيانات بين الشركات قبل التشغيل متعدد الشركات |

## D) الأخطاء والمخاطر

### D.1 إصلاح تم تنفيذه

في مسار `requests.transition` كان الخادم يقرأ الحالة الحالية ثم ينفذ تحديثاً بشرط رقم الطلب فقط. في حال وصول طلبين متزامنين، كان من الممكن أن يمر التحقق الأولي لكليهما قبل أن يكتب أحدهما، ثم يحاول الآخر كتابة انتقال مبني على حالة قديمة. تم تعديل التحديث ليشترط رقم الطلب والحالة السابقة معاً، والتحقق من `affectedRows === 1` قبل كتابة حدث سير العمل وسجل التدقيق. إذا تغيّرت الحالة مسبقاً، تفشل العملية برسالة عربية ولا تُكتب لها سجلات اعتماد أو تدقيق.

تمت إضافة اختبار تراجعي لهذا السيناريو، وأصبحت النتيجة 70 اختباراً ناجحاً بدلاً من 69.

### D.2 مخاطر يجب إغلاقها قبل التشغيل المالي

المخاطرة الأهم المتبقية تشغيلية وليست فشل TypeScript أو Build: وجود بيانات اختبار وعدم تنفيذ اختبار استعادة مستقل. لا ينبغي تنظيف البيانات أو حذف المستخدمين من بيئة حقيقية قبل امتلاك نسخة يمكن التحقق منها وموافقة موثقة.

كما يجب اعتماد سياسة عزل الشركات. الواجهة تدعم تعدد الشركات، لكن بعض استعلامات لوحة المؤشرات وقوائم الطلبات تعمل على بيانات النظام للمستخدم المصادق عليه. إذا كانت الشركة تتطلب عزلاً صارماً بين الشركات، فيجب تحويل ذلك إلى متطلب صريح واختباره قبل التشغيل، لا افتراضه من الواجهة.

سجل التشغيل يحتوي على رسائل متوقعة عند عدم وجود جلسة مثل `Missing session cookie`. لم يظهر في آخر التحقق خطأ JavaScript غير معالج أو فشل شبكة يؤثر على المعاينة. السجلات التاريخية تحتوي على محاولات فشل قديمة أثناء جلسات التطوير، ولا تمثل فشلاً حالياً بعد الإصلاحات الأخيرة.

## E) ترتيب الأولويات المقترح

الأولوية الأولى هي Safety Gate: استخراج نسخة قاعدة البيانات والتخزين ومعلومات مستخدمي المصادقة عبر لوحة Supabase أو صلاحية إدارية رسمية، ثم التحقق من سلامة النسخة، ثم إجراء Restore Test في مشروع غير إنتاجي إن أمكن.

الأولوية الثانية هي اختبار القبول التشغيلي بثلاث جلسات منفصلة: المحاسب ينشئ ويرسل، المراجع المالي يراجع، المدير المالي يعتمد، المدير العام ينفذ، والمدقق يراجع لاحقاً. يجب تكرار الاختبار لطلب صراف وطلب بنك مع حساب بنكي صحيح، ثم معاينة القوالب والطباعة.

الأولوية الثالثة هي تنظيف بيئة الاختبار بعد توثيق النسخة الاحتياطية وتحديد ما سيبقى من إعدادات النظام، مع عدم حذف الحساب الإداري الوحيد قبل توفير بديل مصادق عليه واختباره.

الأولوية الرابعة هي اعتماد سياسة عزل الشركات والصلاحيات على مستوى الشركة، وتحديد ما إذا كان المستخدم يرى كل الشركات أو الشركات المسندة إليه فقط.

الأولوية الخامسة، وهي تحسين غير مانع للتسليم، هي تقليل حجم الحزمة الرئيسية ونقل إعدادات pnpm إلى الصيغة الحديثة بعد اختبار مستقل.

## 2. الملفات التي تم تعديلها في هذه المراجعة

| الملف | التعديل |
|---|---|
| `server/routers.ts` | إضافة حارس ذري لانتقال الحالة، بحيث يشترط الحالة السابقة ويتحقق من عدد الصفوف المتأثرة قبل كتابة سجل workflow/audit |
| `server/requests.workflow.test.ts` | إضافة اختبار يثبت رفض الانتقال المتزامن القديم وعدم كتابة سجل إضافي |
| `server/requests.permissions.test.ts` | تحديث mock ليمثل نجاح التحديث الذري في اختبار الصلاحية القائم |
| `todo.md` | تسجيل بنود المراجعة النهائية وتحديد المنجز والمعلق |
| `FINAL_READINESS_REPORT_AR.md` | هذا التقرير |

لم تُنفذ أي Migration، ولم تُنفذ أوامر SQL، ولم تُغيّر RLS أو قاعدة البيانات أو التخزين أو دورة الاعتماد المقصودة أو نظام المصادقة الأساسي.

## 3. نتائج الفحوصات النهائية

| الفحص | النتيجة |
|---|---|
| TypeScript | ناجح: `pnpm exec tsc --noEmit` |
| Vitest | ناجح: 29 ملفاً، 70 اختباراً |
| Production Build | ناجح: `pnpm run build` |
| Preview | يعمل على رابط المعاينة الحالي في حالة تشغيل |
| Desktop | تمت معاينة صفحة العمل بعرض 1280 بكسل |
| Mobile | تمت معاينة صفحة العمل بعرض 390 بكسل |
| Browser/Network logs | لا يوجد خطأ JavaScript أو فشل شبكة حالي مؤثر؛ `Missing session cookie` عند عدم الدخول متوقع |

## 4. خطة التسليم الآمن للشركة

يجب أولاً تثبيت نسخة الكود الحالية كنقطة استرجاع ومراجعتها من مالك المشروع. بعد ذلك تُنشأ نسخة احتياطية مستقلة للبيانات والتخزين ومعلومات ربط المستخدمين، وتُحفظ خارج بيئة التشغيل، ثم تُفحص سلامتها. لا تُرسل كلمات المرور أو مفاتيح الخدمة أو رموز الجلسات إلى أي شخص أو ملف تقرير.

بعد نجاح النسخة الاحتياطية، تُحدد قائمة بيانات الاختبار المطلوب تنظيفها، وتُعتمد كتابياً، ثم يُنفذ التنظيف في نافذة تشغيل معلنة. يجب الإبقاء على حساب إداري صالح، وإنشاء المستخدمين الحقيقيين بالطريقة الرسمية، ثم ربطهم بسجل الموظفين الداخلي. إضافة سجل موظف لا تنشئ حساب دخول ولا كلمة مرور.

بعد ذلك يُنفذ اختبار قبول قصير موثق بثلاث جلسات مستقلة، ثم تُراجع القوالب الرسمية، ثم يُسلّم دليل التشغيل العربي، ومصفوفة الأدوار، وإجراء النسخ الاحتياطي، وقائمة جهات الاتصال والدعم. لا يُعتبر التسليم المالي مكتملاً لمجرد نجاح Build؛ يجب نجاح اختبار القبول الفعلي وSafety Gate.

## 5. قرار الجاهزية

**القرار التقني:** جاهز للمعاينة التشغيلية والاختبار المنظم، ونجح في الفحوصات البرمجية والبصرية الأساسية.

**القرار التشغيلي:** غير جاهز بعد للاعتماد المالي النهائي أو تسليم بيانات شركة حقيقية حتى تُغلق البنود المعلقة في القسم C، وبالأخص النسخة الاحتياطية القابلة للتحقق واختبار الجلسات الثلاث وتنظيف بيانات الاختبار بموافقة رسمية.

**ما لم يتغير:** لم تُحذف بيانات، ولم تُنشأ قاعدة جديدة، ولم تُنفذ Migration أو SQL، ولم تُعدل RLS أو Storage policies، ولم تُغير دورة الاعتماد أو محرك الطباعة أو نظام المصادقة الأساسي.

## مراجع داخلية

[1]: `server/routers.ts` — مسارات الطلبات والصلاحيات وانتقالات سير العمل والمرفقات.

[2]: `server/requests.workflow.test.ts` — اختبارات دورة الاعتماد والتدقيق والحالة المتزامنة القديمة.

[3]: `server/requests.permissions.test.ts` — اختبارات الصلاحيات على اعتماد الطلب.

[4]: `client/src/_core/hooks/useAuth.ts` — إدارة الجلسة وتسجيل الخروج ومنع إعادة الدخول التلقائي بعد الخروج.

[5]: `todo.md` — سجل المهام والفجوات التشغيلية والاختبارات البشرية المعلقة.
