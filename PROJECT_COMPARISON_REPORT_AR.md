# تقرير مطابقة مشروعي الخزينة الذكية TREZO

## النتيجة التنفيذية

نعم، يمكن جمع أفضل ما في المشروعين داخل مشروع TREZO الحالي، لكن **لا ينبغي دمجهما كنسخة واحدة عبر نسخ الملفات أو تشغيل migrations المشروع المنقول**. المطابقة أثبتت أن المشروعين يعالجان نطاق الخزينة نفسه، لكنهما مبنيان على أساسين تقنيين مختلفين. لذلك فإن الخيار الآمن هو اعتماد مشروع TREZO الحالي باعتباره الأساس التشغيلي، ثم نقل الميزات أو الواجهات المفيدة من المشروع المنقول بعد تكييفها مع عقود الخادم الحالية.

لم يتم تعديل كود المشروع الحالي، ولم تُنفذ أي Migration أو SQL، ولم تُحذف أي بيانات، ولم يُفك أرشيف المشروع المنقول داخل مجلد المشروع التشغيلي.

## 1. هوية المشروعين

| البند | المشروع المنقول من Lovable | مشروع TREZO الحالي |
|---|---|---|
| المصدر المتاح | أرشيف ZIP مرفوع في `/home/ubuntu/upload` | المشروع التشغيلي في `/home/ubuntu/trezo-smart-treasury` |
| الواجهة | React مع TanStack Start وTanStack Router | React 19 مع Vite وواجهة Workspace مخصصة |
| الخادم | TanStack Start/Nitro | Express وtRPC 11 |
| قاعدة البيانات | Supabase PostgreSQL | Drizzle ORM مع MySQL/TiDB |
| المصادقة | Supabase Auth | Manus OAuth وجلسة خادم |
| التخزين | مراجع Supabase وكيانات مرفقات مرتبطة به | مسار تخزين خادمي مع اختبارات تفويض وتنزيل |
| التوجيه | ملفات مستقلة مثل `requests.tsx` و`requests.new.tsx` | `Home.tsx` و`Workspace.tsx` ومكونات متخصصة |
| حالة التشغيل | مصدر مرجعي/أرشيف للمطابقة | النسخة المنشورة والتشغيلية المعتمدة |

## 2. نطاق الميزات المتشابهة

يغطي المشروع المنقول المسارات التالية: الشركات، المستفيدون، قنوات الصرف، التقويم، الطلبات، إنشاء الطلب، تفاصيل الطلب، الإعدادات، وسجل التدقيق. ويستخدم كيانات Supabase بأسماء مثل `companies` و`beneficiaries` و`payment_requests` و`payment_calendar` و`audit_logs` و`attachments`.

أما TREZO الحالي فيغطي هذه المجالات من خلال مسارات tRPC ومكونات اختبارية تشمل الطلبات، دورة الاعتماد، الشركات والمستفيدين والبنوك والقنوات، التقويم، المرفقات، التقارير، أسعار الصرف، سجل التدقيق، الموظفين الداخليين، الأدوار التشغيلية، وتسلسل الرقم المرجعي. النسخة الحالية تحتوي كذلك على اختبارات خادم متعددة للصلاحيات، انتقالات workflow، تفويض المرفقات، الترقيم الذري، التصدير، وتدقيق المستخدمين.

## 3. الفروقات التي تمنع الدمج المباشر

| المجال | التعارض | مستوى الخطر | القرار |
|---|---|---:|---|
| قاعدة البيانات | Supabase/PostgreSQL مقابل Drizzle/MySQL/TiDB | مرتفع جداً | عدم نقل migrations مباشرة |
| المصادقة | Supabase Auth مقابل Manus OAuth | مرتفع جداً | عدم نقل session أو RLS أو auth hooks |
| الصلاحيات | سياسات Supabase وRLS مقابل حراسة tRPC الخادمية | مرتفع جداً | إعادة صياغة أي صلاحية داخل الخادم الحالي |
| التخزين | نمط Supabase Storage مقابل التخزين الخادمي الحالي | مرتفع | نقل التصميم الوظيفي فقط بعد مراجعة التفويض |
| التوجيه | TanStack Router مقابل shell Workspace | متوسط | نقل الشاشة إلى مكوّن داخل Workspace، لا نسخ route مباشرة |
| النماذج | عقود Supabase المباشرة مقابل عقود tRPC typed | مرتفع | إعادة ربط الحقول بإجراءات tRPC الحالية |
| دورة الاعتماد | قد تختلف حالات المشروعين وترتيبها | مرتفع جداً | الحفاظ على دورة TREZO الحالية وعدم استبدالها |
| الترقيم | صيغة المشروع المنقول مختلفة عن تسلسل TREZO الذري | مرتفع | الحفاظ على تسلسل TREZO وعدم استيراد الرقم المرجعي |
| البيانات | احتمال اختلاف المعرفات والعلاقات والبيانات التجريبية | مرتفع | عدم دمج بيانات المشروعين تلقائياً |

## 4. ما يمكن الاستفادة منه بأمان

يمكن الاستفادة من **أفكار الواجهة وتجربة المستخدم** في المشروع المنقول، مثل تقسيم صفحات الشركات والمستفيدين والتقويم، أو بعض نصوص الحقول والتسميات، بعد إعادة بنائها داخل مكونات TREZO الحالية. ويمكن أيضاً مقارنة نماذج الطلبات والفلاتر والتقارير لاستخراج الحقول التي قد تكون مفيدة، بشرط أن تمر كل إضافة عبر عقود tRPC والصلاحيات الموجودة.

يمكن نقل منطق عرض عربي مستقل، مثل تحويل المبلغ إلى كلمات أو تنظيم الفلاتر، إذا ثبت أنه أفضل من الموجود حالياً، لكن يجب استخدام وظائف TREZO الحالية أو اختبار البديل قبل استبداله. كما يمكن استخدام المشروع المنقول كمرجع بصري أو وظيفي لاختبار الفجوات، وليس كمصدر مباشر لقاعدة البيانات أو المصادقة.

## 5. ما لا ينبغي نقله

لا ينبغي نقل مجلد `supabase/migrations`، أو إعدادات Supabase، أو استدعاءات `supabase.auth`، أو سياسات RLS، أو أي بيانات موجودة في المشروع المنقول إلى مشروع TREZO الحالي. كذلك لا ينبغي نسخ ملفاته إلى `client/src` أو `server` بشكل شامل، لأن ذلك قد يسبب تعارضاً في التوجيه والمصادقة والعقود ويؤثر في دورة الاعتماد الحالية.

ولا ينبغي استبدال نظام TREZO الحالي بنظام المشروع المنقول لمجرد أن الأخير يحتوي على صفحات أكثر تفصيلاً. معيار الاختيار هو سلامة التشغيل، الصلاحيات الخادمية، توافق قاعدة البيانات، الاختبارات، وعدم فقدان البيانات.

## 6. خريطة الدمج الآمنة المقترحة

### المرحلة الأولى: اعتماد الأساس

يبقى مشروع TREZO الحالي هو المشروع الأساسي، وتبقى قاعدة بياناته ومصادقته ودورة اعتماده وترقيمه دون تغيير. لا يتم دمج أي ملف قبل إنشاء نقطة تحقق مستقلة ومقارنة التغييرات.

### المرحلة الثانية: اختيار ميزة واحدة

يتم اختيار ميزة محددة من المشروع المنقول، مثل تحسين صفحة الشركات أو نموذج الطلب أو فلاتر التقارير. لا يتم اختيار أكثر من وحدة في الدفعة الأولى حتى يكون الاختبار والعزل واضحين.

### المرحلة الثالثة: إعادة البناء داخل TREZO

تُعاد كتابة الميزة المختارة داخل مكونات TREZO الحالية، وتستخدم tRPC وDrizzle والصلاحيات الموجودة. لا تُستورد استدعاءات Supabase ولا route files الخاصة بـTanStack Start.

### المرحلة الرابعة: الاختبار

بعد كل ميزة، يجب تشغيل TypeScript وVitest وProduction Build، ثم اختبار الواجهة على الكمبيوتر والهاتف، مع اختبار الصلاحيات وعدم تغيير دورة الاعتماد أو الترقيم أو البيانات.

### المرحلة الخامسة: قرار الاستمرار

إذا نجحت الميزة دون تعارض أو تراجع، تُحفظ كنقطة تحقق. وإذا ظهر تعارض، تُعاد المحاولة من نقطة التحقق السابقة بدلاً من تعديل قاعدة البيانات أو حذف ملفات.

## 7. توصية القرار

أوصي بعدم تنفيذ دمج شامل. أوصي بتنفيذ **مطابقة انتقائية ثم دمج ميزة واحدة في كل مرة**. وبالنسبة للخطوة الأولى، أفضل مرشح هو مقارنة نموذج الطلب وتقسيم الحقول والفلاتر في المشروع المنقول مع `OperationalRequestModal` و`RequestDetailPanel` و`Workspace` في TREZO، دون المساس بدورة الاعتماد أو قاعدة البيانات.

إذا وافقت على هذا المسار، سأبدأ بميزة واحدة فقط، وسأقدم قبل التنفيذ قائمة الملفات التي ستتأثر، ثم أنتظر موافقتك على تلك القائمة. لن يتم تشغيل أي Migration أو SQL ولن يتم حذف أي بيانات.

## حالة المهمة الحالية

| العنصر | الحالة |
|---|---|
| تحديد المشروعين | مكتمل |
| مقارنة البنية والتقنيات | مكتمل |
| تقييم تعارض قاعدة البيانات والمصادقة | مكتمل |
| تنفيذ أي دمج | لم يبدأ |
| تعديل كود TREZO | لم يحدث ضمن هذه المطابقة |
| تغيير قاعدة البيانات أو البيانات | لم يحدث |
| القرار المطلوب من المستخدم | اختيار أول ميزة يراد مطابقتها أو الموافقة على البدء بنموذج الطلب |

**الخلاصة:** يمكن الجمع بين المشروعين على مستوى الخبرات والميزات، لكن ليس على مستوى البنية التحتية. مشروع TREZO الحالي هو الأساس الصحيح للتشغيل، والمشروع المنقول يُستخدم كمصدر مقارنة وانتقاء وظيفي.
