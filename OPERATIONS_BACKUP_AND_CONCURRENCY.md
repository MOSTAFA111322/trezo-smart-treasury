# TREZO — دليل التشغيل المؤسسي

## 1. حدود هذه الوثيقة

هذه الوثيقة توثق إجراءات تشغيل آمنة لنظام TREZO Smart Treasury. لا تنفذ النسخ الاحتياطي بنفسها، ولا تحتوي على مفاتيح أو كلمات مرور أو رموز وصول، ولا تستبدل النسخ الرسمية التي ينفذها مالك قاعدة البيانات ومزود الاستضافة.

## 2. النسخ الاحتياطي الموصى به

ينبغي أن يحتفظ مالك النظام بثلاثة أنواع مستقلة من النسخ: نسخة قاعدة البيانات، ونسخة كائنات التخزين والمرفقات، ونسخة إعدادات الهوية والمستخدمين غير السرية اللازمة لإعادة الربط. يجب حفظ النسخ في مخزن مشفر منفصل عن الإنتاج، مع تقييد الوصول إلى المسؤولين المخولين فقط.

### قاعدة البيانات

يُنفذ النسخ من لوحة مزود قاعدة البيانات أو من PostgreSQL/MySQL CLI بحسب المزود الفعلي. قبل التنفيذ يجب تحديد نافذة زمنية قصيرة، وتوثيق وقت النسخ، واسم البيئة، ونطاق الجداول، وعدم تنفيذ أي `DROP` أو `TRUNCATE` أو تعديل على الإنتاج. يجب الاحتفاظ بملف المخطط وملف البيانات أو snapshot رسمي، مع checksum للملفات.

### التخزين والمرفقات

يجب تصدير قائمة metadata للمرفقات، بما في ذلك المفتاح، الحجم، النوع، المالك، والمرجع إلى طلب الصرف، ثم نسخ الكائنات نفسها إلى مخزن منفصل مشفر. لا يكفي حفظ روابط مؤقتة؛ يجب حفظ المفاتيح الأصلية وmetadata، مع اختبار تنزيل عينة من الكائنات والتحقق من checksum.

### الهوية والمستخدمون

يُحفظ فقط ما يلزم للربط المستقبلي: المعرف الخارجي، البريد أو الاسم إذا كانا جزءاً من سجل العمل، الدور التشغيلي، حالة الحساب، وتاريخ التصدير. لا تُحفظ كلمات المرور، أو access tokens، أو refresh tokens، أو JWT، أو مفاتيح الخدمة، أو connection strings ضمن الوثيقة أو مستودع الكود.

## 3. التحقق من سلامة النسخ

بعد كل تصدير يجب حساب SHA-256 لكل ملف، حفظ سجل checksum في مكان منفصل، التأكد من أن حجم الملف غير صفري، وفتح نسخة القراءة أو فهرس التصدير. يجب اختبار استرجاع عينة من الصفوف وكائنات التخزين، ومقارنة عدد الجداول/الكائنات/المستخدمين مع تقرير المصدر، وتوثيق أي فرق بدلاً من تجاهله.

## 4. اختبار الاستعادة خارج الإنتاج

يُنشأ مشروع أو بيئة اختبار مستقلة. تُستعاد قاعدة البيانات أولاً، ثم تُستعاد metadata وكائنات التخزين، ثم تُشغل نسخة التطبيق بإعدادات الاختبار فقط. يجب اختبار تسجيل الدخول، قراءة الشركات والمستفيدين والبنوك، إنشاء طلب تجريبي غير متصل بالإنتاج، قراءة المرفقات، دورة الحالات، سجل التدقيق، والتقارير والطباعة. بعد الاختبار تُحذف بيئة الاختبار وفق سياسة الاحتفاظ المعتمدة، ولا يُعاد توجيه الإنتاج إليها.

## 5. التزامن والتشغيل المتعدد

يعتمد إنشاء الرقم المرجعي على معاملة قاعدة بيانات وحجز ذري للقيمة التالية، مع اختبار تنافس يثبت أن طلبين متزامنين لا يحصلان على الرقم نفسه. تعتمد انتقالات دورة الاعتماد على تحقق الصلاحية والحالة الحالية ثم كتابة سجل workflow وسجل audit. لذلك يمكن تشغيل عدة محاسبين بالتوازي، بشرط أن تكون كل عملية حساسة عبر الخادم وأن تُراجع أخطاء التعارض وتُعاد المحاولة من خلال واجهة واضحة.

### اختبار قبول لعشرة محاسبين

يُستخدم اختبار تحميل في بيئة اختبار فقط لإرسال طلبات متزامنة بحسابات منفصلة، مع التحقق من عدم تكرار الرقم المرجعي، وعدم فقدان سجل التدقيق، وعدم قبول انتقال غير مسموح، وعدم ظهور مرفق لمستخدم غير مخول. لا تُستخدم بيانات عملاء حقيقية ولا تُجرى هذه التجربة على الإنتاج.

## 6. سياسة الاحتفاظ والاستعادة

يُوصى بنسخ يومية قصيرة الاحتفاظ، ونسخ أسبوعية أطول احتفاظ، ونسخة شهرية مؤرشفة، مع اختبار استعادة دوري موثق. يجب تحديد مسؤول النسخ، مسؤول الاستعادة، زمن الاستعادة المستهدف، ونقطة الاستعادة المستهدفة قبل اعتماد النظام للإنتاج.

## 7. ما يجب عدم فعله

لا تُطبع الأسرار في السجلات أو التقارير. لا تُرسل نسخ قاعدة البيانات عبر البريد العادي. لا تُخلط نسخ من عمليات تصدير مختلفة. لا تُنفذ استعادة فوق الإنتاج قبل نجاح اختبار مستقل وموافقة صريحة. لا تُعتبر نسخة الكود وحدها نسخة كاملة من النظام.
