# خطة الاختبار اليدوي — البنك والحساب البنكي وقناة الصراف

## النطاق

التحقق من أن البنك، والحساب البنكي للمستفيد، وقناة الصرف كيانات منفصلة في نموذج إنشاء طلب الصرف، وأن قناة الصراف النقدية لا تتطلب بنكاً أو حساباً بنكياً.

## المتطلبات المسبقة

يجب أن توجد شركة فعالة، ومستفيد فعّال، وقناة صراف فعّالة، وقناة بنكية فعّالة، وبنك فعّال واحد على الأقل. لاختبارات الحساب البنكي يجب ربط حساب بنكي فعّال بالمستفيد المختبر والبنك المختار. يجب تنفيذ الاختبارات في بيئة تجريبية وبطلب غير مالي حقيقي.

## MT-01 — بنك موجود بلا حساب مرتبط

الخطوات: افتح إنشاء طلب صرف، أدخل العنوان والمبلغ والشركة والمستفيد، اختر «بنك» من نوع جهة الصرف، ثم اختر قناة بنكية. اختر بنكاً فعالاً لا يوجد للمستفيد حساب مرتبط به.

النتيجة المتوقعة: يظهر البنك في قائمة «البنك»، ولا يظهر حساب غير مرتبط به. يظهر تنبيه يفيد بعدم وجود حساب بنكي لهذا المستفيد في البنك المختار. يبقى زر إنشاء المسودة غير قادر على إنشاء طلب مكتمل، وتظهر خانتا «البنك» و«الحساب البنكي» ضمن الحقول المطلوبة. لا يحدث أي تغيير في قاعدة البيانات.

## MT-02 — بنك مع حساب مرتبط

الخطوات: أكمل نفس البيانات، اختر «بنك»، اختر البنك المرتبط بالمستفيد، ثم اختر حساب المستفيد البنكي الظاهر، وأكمل السنة المالية والعملة.

النتيجة المتوقعة: يظهر البنك المضاف في قائمة مستقلة، وتظهر فقط حسابات المستفيد المرتبطة بالبنك المختار. يصبح الطلب جاهزاً للحفظ. عند إنشاء المسودة يرسل النموذج `channelId` للقناة البنكية و`bankAccountId` للحساب، ولا يرسل `bankId` كحقل حفظ مستقل إذا كان عقد الخادم الحالي يعتمد على `bankAccountId`. لا يتغير مسار الاعتماد.

## MT-03 — قناة الصراف النقدية

الخطوات: افتح طلباً جديداً، أدخل البيانات الأساسية، اختر «صراف» من نوع جهة الصرف، ثم اختر قناة صراف فعّالة.

النتيجة المتوقعة: يظهر توضيح بأن قناة الصراف النقدية لا تتطلب بنكاً أو حساباً بنكياً. يكون حقل الحساب البنكي معطلاً أو غير مطلوب، ولا تظهر مطالبة باختيار بنك. عند الحفظ يرسل النموذج قناة الصراف، ولا يرسل `bankAccountId` فعلياً؛ وجود قيمة `undefined` في الكائن البرمجي مقبول، أما إرسال رقم حساب بنكي فغير مقبول.

## MT-04 — تغيير المستفيد بعد اختيار البنك والحساب

الخطوات: اختر «بنك»، ثم اختر البنك، ثم اختر حساب المستفيد. بعد ذلك غيّر المستفيد إلى مستفيد آخر.

النتيجة المتوقعة: يصفر النموذج كلاً من البنك والحساب البنكي تلقائياً، ولا يبقى الحساب القديم محدداً. يجب إعادة اختيار البنك والحساب وفق المستفيد الجديد. يمنع النموذج إرسال حساب تابع للمستفيد السابق.

## معيار القبول

ينجح الإصلاح إذا نجحت الحالات الأربع دون خلط بين `channelId` و`bankId` و`bankAccountId`، ودون تعديل بيانات أو Migration، ودون تجاوز التحقق الخلفي أو دورة الاعتماد.
